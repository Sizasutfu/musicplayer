// hooks/usePlaylists.ts
// Re-export from context so existing imports keep working.
// The single source of truth lives in context/PlaylistsContext.tsx.
export { usePlaylists } from '../context/PlaylistsContext';